# WebSocket del servidor de ajedrez.
# Dejalo SIN definir para el deploy unificado (el server sirve el web y el WS se
# deriva del origen). Definilo solo si el web corre en un origen distinto al server
# (p. ej. Vite en :5173 durante dev → ver web/.env.development).
# VITE_WS_URL=ws://localhost:8787
