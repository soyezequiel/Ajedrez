# Dev con Vite (:5173) y el server aparte (:8787): apuntamos el WS al server.
# En el build de producción esta var queda sin definir y el WS se deriva del
# origen (deploy unificado). Ver web/src/config.ts.
VITE_WS_URL=ws://localhost:8787
